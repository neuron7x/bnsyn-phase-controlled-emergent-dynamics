# BN-Syn Offline Demo (Deterministic)

This demo runs fully offline and writes reproducible artifacts.

## Canonical demo command

```bash
python -m bnsyn demo-product
python -m bnsyn validate-bundle artifacts/canonical_run
