# Gate failure
- failing gate: make test
- first failing error excerpt (verbatim):
```
+ make test
```
- minimal fix plan:
  1. Address lint/type/build failures in changed files only.
  2. Re-run make test after updates.
