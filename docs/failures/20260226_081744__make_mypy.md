# Gate failure
- failing gate: make mypy
- first failing error excerpt (verbatim):
```
src/bnsyn/web/deps.py:20: error: Returning Any from function declared to return "Settings"  [no-any-return]
```
- minimal fix steps:
  1. Add explicit cast for request app state settings.
  2. Re-run make mypy.
