# Gate failure
- failing gate: make test
- first failing error excerpt (verbatim):
```
FAILED tests/test_web_auth.py::test_logout_revokes_token[asyncio] - assert 401 == 200
```
- minimal fix steps:
  1. Remove unsupported `current_time` kwarg path from JWT decode.
  2. Enforce token expiration against logical clock in dependency layer after claim validation.
  3. Re-run make test.
