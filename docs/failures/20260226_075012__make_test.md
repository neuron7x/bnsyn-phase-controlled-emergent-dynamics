# Gate failure
- failing gate: make test
- first failing error excerpt (verbatim):
```
FAILED tests/test_entropy_gate.py::test_entropy_gate_no_regressions - AssertionError: ENTROPY_GATE_BLOCKED:
```
- minimal fix steps:
  1. Remove new time-call offenders from added web module code.
  2. Remove any newly introduced unpinned dependency pattern causing entropy regression.
  3. Re-run make test.
